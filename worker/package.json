{
  "name": "mentenna-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "npx prisma generate --schema=../prisma/schema.prisma",
    "dev": "npm run generate && tsx watch src/index.ts",
    "start": "npm run generate && tsx src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "dotenv": "^16.4.5",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/uuid": "^10.0.0",
    "prisma": "^6.17.1",
    "tsx": "^4.19.1",
    "typescript": "^5"
  }
}